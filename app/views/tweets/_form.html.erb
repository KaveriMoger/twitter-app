<h1>New Tweet Form</h1>
<%= form_for (@tweet) do |f| %>
  <%= f.hidden_field  :user_id, value:current_user.id %>

  <%= f.label :content %>
  <%= f.text_field :content %>
  <br><br>
  
  <%= f.submit %>
<% end %>